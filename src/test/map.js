import test from 'tape'

import { MapNode } from '../lib/map'
import { elementMap, allNodes } from '../lib/util'

import {
  createElements, createElementNode, createElementNodes,
  subElements, mapNames, mapCounts
} from './helper'

test('map node test', assert => {

})
